import Reac from 'react';
import styled from "styled-components";

const Navbar = () => {
    return (
        <s.Container>
            <TextTitle>
                Navbar
            </TextTitle>
        </s.Container>
    )
}

export default Navbar